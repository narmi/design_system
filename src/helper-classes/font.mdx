import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import {
  fontColor as fontColorHelpers,
  fontSize as fontSizeHelpers,
  fontWeight as fontWeightHelpers,
  fontFamily as fontFamilyHelpers,
  whiteSpace,
} from "dist/docs/classManifest.json";
import ClassExample from "helpers/ClassExample";
import * as FontStories from "./font.stories";

<Meta of={FontStories} />

# Font Color

Adjusts font color.

<Canvas of={FontStories.FontColor} />

# Font Size

Adjusts font size.

<Canvas of={FontStories.FontSize} />

# Font Weight

Adjusts font weight.

<Canvas of={FontStories.FontWeight} />

# Font Family

Adjusts font family.

<Canvas of={FontStories.FontFamily} />

# Whitespace control

Whitespace helpers adjust text truncation and wrapping behavior.
Single line and multiline truncation are both supported.

<Canvas of={FontStories.WhitespaceControl} />
