import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import { alignChild, rotate } from "dist/docs/classManifest.json";
import ClassExample from "helpers/ClassExample";
import * as PositionStories from "./position.stories";

<Meta of={PositionStories} />

# Child Alignment

Aligns children of element using flexbox. Useful for quick center alignment.

<Canvas of={PositionStories.ChildAlignment} />

# Rotation

<Canvas of={PositionStories.Rotation} />
