import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import { overflow as overflowHelpers } from "dist/docs/classManifest.json";
import ClassExample from "helpers/ClassExample";
import * as ScrollStories from "./scroll.stories";

<Meta of={ScrollStories} />

# Controlling Overflow

Disable scrolling with `overflow--hidden`.
Enable scrolling with `overflow--scroll`.

Useful for locking body scroll when modals are open.

<Canvas of={ScrollStories.ControllingOverflow} />
