import { Meta, Story } from "@storybook/addon-docs";
import { font } from "dist/tokens/js/manifest";
import TokenTable, { toTokenRows } from "helpers/TokenTable";

<Meta title="Design Tokens/Font" />

# Font Tokens

## Font Color

<Story name="Color">
  <TokenTable previewType="color" rows={toTokenRows(font, "color")} />
</Story>

## Font Size

<Story name="Size">
  <TokenTable previewType="fontSize" rows={toTokenRows(font, "size")} />
</Story>

## Font Family

<Story name="Family">
  <TokenTable previewType="fontFamily" rows={toTokenRows(font, "family")} />
</Story>

## Font Weight

<Story name="Weight">
  <TokenTable previewType="fontWeight" rows={toTokenRows(font, "weight")} />
</Story>

## Font Line Height

<Story name="Line Height">
  <TokenTable rows={toTokenRows(font, "lineHeight")} />
</Story>
